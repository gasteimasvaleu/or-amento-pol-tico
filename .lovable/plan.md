

## Plano: Esconder card de importação CSV

Remover o componente `<ImportCSV />` da página `DadosEleitorais.tsx` comentando ou removendo a linha de renderização. O componente e seu código ficam preservados para uso futuro.

### Alteração em `src/pages/DadosEleitorais.tsx`

- Remover a linha `<ImportCSV />` e seu import correspondente

